---
import { getCollection } from "astro:content";
import Layout from "@layouts/Layout.astro";
import Container from "@components/container.astro";
import Sectionhead from "@components/sectionhead.astro";

// Filter blog entries with 'draft: false' & date before current date
const publishedBlogEntries = await getCollection("blog", ({ data }) => {
  return !data.draft && data.publishDate < new Date();
});

// Sort content entries by publication date
publishedBlogEntries.sort(function (a, b) {
  return b.data.publishDate.valueOf() - a.data.publishDate.valueOf();
});
---

<Layout
  title="Blog"
  description="Articles, tutorials and notes by Hadouin Leroy on creative development, 3D workflows, Blender, web engineering and onchain gaming."
  keywords={["Hadouin Leroy blog", "Blender tutorials", "creative development", "web engineering"]}
>
  <Container>
    <Sectionhead>
      <Fragment slot="title">My Blog</Fragment>
      <Fragment slot="desc">
        I write about things I learn and projects I work on.
      </Fragment>
    </Sectionhead>
    <main class="mt-16">
      <ul class="grid gap-16 max-w-4xl mx-auto">
        {
          publishedBlogEntries.map((blogPostEntry, index) => (
            <li>
              <a href={`/blog/${blogPostEntry.id}`}>
                <div class="grid md:grid-cols-2 gap-5 md:gap-10 items-center">
                  <div class="overflow-hidden rounded-md">
                    {blogPostEntry.data.image.src.endsWith(".mp4") ? (
                      <video
                        src={blogPostEntry.data.image.src}
                        autoplay
                        muted
                        playsinline
                        class="w-full aspect-video object-cover"
                        data-parallax="0.05"
                      />
                    ) : (
                      <img
                        src={blogPostEntry.data.image.src}
                        alt={blogPostEntry.data.image.alt}
                        loading={index <= 2 ? "eager" : "lazy"}
                        decoding={index <= 2 ? "sync" : "async"}
                        class="w-full aspect-video object-cover"
                        data-parallax="0.05"
                      />
                    )}
                  </div>
                  <div>
                    <span class="text-blue-400 uppercase tracking-wider text-sm font-medium">
                      {blogPostEntry.data.category}
                    </span>

                    <h2 class="scramble-hover text-3xl font-semibold leading-snug tracking-tight mt-1 ">
                      {blogPostEntry.data.title}
                    </h2>

                    <div class="flex gap-2 mt-3">
                      <span class="text-gray-400">
                        {blogPostEntry.data.author}
                      </span>
                      <span class="text-gray-400">• </span>
                      <time
                        class="text-gray-400"
                        datetime={blogPostEntry.data.publishDate.toISOString()}>
                        {blogPostEntry.data.publishDate.toDateString()}
                      </time>
                    </div>
                  </div>
                </div>
              </a>
            </li>
          ))
        }
      </ul>
    </main>
  </Container>
</Layout>
