---
import { getCollection } from "astro:content";
import BlogDetail from "@components/blog-detail.astro";

export async function getStaticPaths() {
  const enEntries = await getCollection("blog", ({ id, data }) => {
    return id.startsWith("en/") && !data.draft;
  });
  return enEntries.map((entry) => {
    const slug = entry.id.slice(3);
    return {
      params: { slug },
      props: { entry, slug },
    };
  });
}

const { entry, slug } = Astro.props;
---

<BlogDetail locale="en" slug={slug} entry={entry} />
